import { defineMessages } from 'react-intl';

export const scope = 'front.components.HeaderTool';

export default defineMessages({
  moduleInitial: {
    id: `${scope}.module.initial`,
    defaultMessage: 'Geral',
  },
  menuInitial: {
    id: `${scope}.menu.initial`,
    defaultMessage: 'Inicio',
  },
});
